import PlaceholderScreen from "@/components/app-shell/PlaceholderScreen";

export default function AssignmentsPage() {
  return (
    <PlaceholderScreen
      title="Assignments"
      description="Reassign batch/case ownership with optimistic concurrency and stale-view warnings. Stage 8 (T8) builds this."
      wave="Stage 8"
    />
  );
}
