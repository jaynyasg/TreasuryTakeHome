import PlaceholderScreen from "@/components/app-shell/PlaceholderScreen";

export default function SettingsPage() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Kill switches and operational settings for durable batch intake, worker, model calls, replay, exports, and purge. Stage 8 (T8) builds this."
      wave="Stage 8"
    />
  );
}
